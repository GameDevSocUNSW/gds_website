#/bin/sh

rake assets:precompile && rake db:migrate && rake db:migrate RAILS_ENV=production && git add . && git co "$1" && git push origin master && git push heroku master && heroku run rake db:migrate && heroku restart
